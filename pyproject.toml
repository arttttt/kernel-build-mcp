[project]
name = "kernel-build-mcp"
version = "0.1.0"
description = "MCP server for remote Linux kernel cross-compilation"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
]

[project.scripts]
kernel-build-mcp = "kernel_build_mcp.server:main"
